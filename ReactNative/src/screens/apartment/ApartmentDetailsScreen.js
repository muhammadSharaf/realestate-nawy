import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';

function ApartmentDetailsScreen({navigation}) {
    return (
        <SafeAreaView edges={['top', 'left', 'right']}>

        </SafeAreaView>
    );
}

export default ApartmentDetailsScreen;
